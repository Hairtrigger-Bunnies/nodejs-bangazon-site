extends index.pug

block content
  h2 Your Cart Items:
    ul
      each product in cartProducts
        li 
          h4= product.title
          h5= product.description
          h5 $#{product.price}
          h5 #{product.quantityInCart} currently in your cart.
    button(type="button") 